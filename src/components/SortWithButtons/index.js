// import {useMemo} from 'react'
// import FilteredCarsContext from '../context/FilteredCarsContext';

// function SortWithButtons() {

//   const { products } = props;
//   const [sortConfig, setSortConfig] = React.useState(null);
  
//   return (
//     <div>
//       <button onClick={() => filter('moreExpensive')}>Mas caro</button>
//       <button onClick={() => filter('cheaper')}>Mas barato</button>
//       <button onClick={() => filter('newest')}>Mas nuevo</button>
//       <button onClick={() => filter('oldest')}>Mas viejo</button>
//     </div>
//   );

// }

// export default SortWithButtons